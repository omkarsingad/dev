apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus-dep
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/omkarsingad/modules.git
    targetRevision: main
    path: prometheus/chart/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring   
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
